import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'eventsDDR2021',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
